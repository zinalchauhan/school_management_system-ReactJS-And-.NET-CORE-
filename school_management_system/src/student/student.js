import { BrowserRouter, Route, Switch } from "react-router-dom";

function student() {
    return (
        <BrowserRouter>
        <switch>
            
        </switch>
        </BrowserRouter>
    );
}